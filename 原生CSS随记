使用 calc() 函数计算 <div> 元素的宽度:
需要注意的是，运算符前后都需要保留一个空格，例如：width: calc(100% - 10px)；calc()函数支持 "+", "-", "*", "/" 运算；
100%指的是什么



z-index得相对定位

在使用z-index前，先将元素定位，例如position:relative;然后再后面再添加z-index:2000就OK了。
